import AdminContainer from '@/containers/admin/Admin';

export const metadata = {
  title: 'NextAuth | Admin',
};

const AdminPage = () => {
  return <AdminContainer />;
};

export default AdminPage;
